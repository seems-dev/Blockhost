from __future__ import annotations

# Setup-stage placeholder.
#
# agent.md describes a multi-tier backup strategy (VM -> B2 -> device + monthly archives).
# We'll wire this up after the core API + DB + orchestration loop is stable.
